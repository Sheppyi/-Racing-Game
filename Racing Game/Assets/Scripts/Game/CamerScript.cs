using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CamerScript : MonoBehaviour {

    /*
    GameObject player;
    Quaternion rotation;

    private void Start() {
        try {
            player = GameObject.FindGameObjectWithTag("Player1Spine");
        }
        catch {
            Debug.Log("Camera cannot locate player");
        }
    }
    */
}
